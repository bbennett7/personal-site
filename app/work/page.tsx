import { PageHeader } from '@/components/PageHeader';

export default function Work() {
  return (
    <PageHeader
      label="// Work"
      heading={<>Work I&apos;ve owned on teams.</>}
      tagline="Six years of production code at companies building real things under real constraints."
    />
  );
}
